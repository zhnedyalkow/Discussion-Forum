mixin login-modal()
    #login-modal.modal.fade(role='dialog')
        .modal-dialog
            .modal-content
                    button.close(type='button', data-dismiss='modal') ×
                    img(src="/static/images/myForum.png", alt="")
                    h3 
                        ="Welcome to MyForum, " + user.username + " !"
                    h3
                        ="We're glad that you're with us, let's start "
                        <i class="fa fa-smile-o main-color-bg" aria-hidden="true"></i>
                    button.btn.btn-default.btn-info(type='button', data-dismiss='modal') 
                        | Close
